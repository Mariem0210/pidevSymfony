<form method="post" action="{{ path('app_equipe_delete', {'ideq': equipe.ideq}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipe.ideq) }}">
    <button class="btn">Delete</button>
</form>

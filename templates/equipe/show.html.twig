{% extends 'base.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ideq</th>
                <td>{{ equipe.ideq }}</td>
            </tr>
            <tr>
                <th>Nom_equipe</th>
                <td>{{ equipe.nomEquipe }}</td>
            </tr>
            <tr>
                <th>Date_creation</th>
                <td>{{ equipe.dateCreation ? equipe.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Idu</th>
                <td>{{ equipe.idu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipe_index') }}">back to list</a>

    <a href="{{ path('app_equipe_edit', {'ideq': equipe.ideq}) }}">edit</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}

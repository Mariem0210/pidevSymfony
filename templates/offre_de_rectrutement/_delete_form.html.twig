<form method="post" action="{{ path('app_offre_de_rectrutement_delete', {'ido': offre_de_rectrutement.ido}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre_de_rectrutement.ido) }}">
    <button class="btn">Delete</button>
</form>

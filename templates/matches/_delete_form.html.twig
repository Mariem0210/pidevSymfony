<form method="post" action="{{ path('app_matches_delete', {'idm': match.idm}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ match.idm) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Transfert{% endblock %}

{% block body %}
    <h1>Transfert</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idtr</th>
                <td>{{ transfert.idtr }}</td>
            </tr>
            <tr>
                <th>Idu</th>
                <td>{{ transfert.idu }}</td>
            </tr>
            <tr>
                <th>Ancienne_equipe</th>
                <td>{{ transfert.ancienneEquipe }}</td>
            </tr>
            <tr>
                <th>Nouvelle_equipe</th>
                <td>{{ transfert.nouvelleEquipe }}</td>
            </tr>
            <tr>
                <th>Montantt</th>
                <td>{{ transfert.montantt }}</td>
            </tr>
            <tr>
                <th>Datet</th>
                <td>{{ transfert.datet ? transfert.datet|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_transfert_index') }}">back to list</a>

    <a href="{{ path('app_transfert_edit', {'idtr': transfert.idtr}) }}">edit</a>

    {{ include('transfert/_delete_form.html.twig') }}
{% endblock %}

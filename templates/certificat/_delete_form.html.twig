<form method="post" action="{{ path('app_certificat_delete', {'id': certificat.id}) }}" 
      onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce certificat ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ certificat.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>